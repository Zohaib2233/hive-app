// ignore_for_file: constant_identifier_names

import 'package:flutter/widgets.dart';

class AppSizes {
  static const DEFAULT_PADDING = EdgeInsets.symmetric(
    horizontal: 30,
    vertical: 35,
  );
  static const DEFAULT_HORIZONTAL = EdgeInsets.symmetric(
    horizontal: 30,
  );
  static const DEFAULT_VERTICAL = EdgeInsets.symmetric(
    vertical: 35,
  );

  static const double verticalPadding = 8;
  static const double normalHorizontalSpacing = 6;
}
